{
  "name": "empire-high-stakes-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EMPIRE: High Stakes - Cross-Platform Real-World Business Simulation MMO",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=apps/web-preview run dev",
    "build:core": "npm --workspace=packages/core-simulation run build",
    "build:web": "npm --workspace=apps/web-preview run build",
    "build": "npm run build:core && npm run build:web"
  }
}