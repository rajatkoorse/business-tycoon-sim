{
  "name": "empire-high-stakes-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EMPIRE: High Stakes - Cross-Platform Real-World Business Simulation MMO",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=apps/web-preview run dev",
    "build": "npm --workspaces run build"
  }
}